#include "union_find.cpp"


/*
int main() {

    UnionFind<int> numbers(12);

    for (int i = 0; i < 12; ++i) {
        numbers.insert(i + 1);
    }

    numbers.printItems();

    numbers.unionItems(2, 10);
    numbers.unionItems(7, 5);
    numbers.unionItems(6, 1);
    numbers.unionItems(3, 4);
    numbers.unionItems(5, 11);
    numbers.unionItems(7, 8);
    numbers.unionItems(7, 3);
    numbers.unionItems(12, 2);
    numbers.unionItems(9, 6);


    numbers.findPrint(6);
    numbers.findPrint(3);
    numbers.findPrint(11);
    numbers.findPrint(9);


    numbers.printItems();
}
*/

