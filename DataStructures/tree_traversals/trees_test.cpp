

#include "BinarySearchTree.h"
#include "BinarySearchTree.cpp"
#include <functional> //less<T> <-> operator<

/*
int main() {

    BinarySearchTree<int> tree;

    tree.insert(1);
    tree.insert(2);
    tree.insert(5);
    tree.insert(4);
    tree.insert(3);

    std::cout << "the size of the tree is " << tree.getSize() << std::endl;
    std::cout << "the min is " << tree.findMin() << std::endl;
    std::cout << "the max is " << tree.findMax() << std::endl;
    std::cout << "trying to find the value of five " << tree[5] << std::endl;
    tree.remove(2);

    tree.print();


}

*/
